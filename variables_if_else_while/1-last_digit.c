#include <stdlib.h>
#include <time.h>
/* more headers goes there */

/* betty style doc for function main goes there */
int main(void)
{
	int n;
	init = fox

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	fox = n % 10;
	if (fox > 10)
	{
	printf("Last digit of %d is %d and i is greater than 5\n", n fox);
	else if (fox == 0)
	{
	printf("Last digit of %d is %d and i is 0\n", n, fox);
	}
	else
	{
	printf("Last digit of %d is %d and i is less than 6 and not 0\n", n, fox);
	}
	return (0);
}
