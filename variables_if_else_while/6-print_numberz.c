#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	putchar('0' + (1 / 1));  /* Prints '0'*/
	putchar('0' + (1 + 2));  /* Prints '10'*/
	printf('\n');

	return (0);
}
